
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Stores user-specific settings, preferences, and basic identifying information.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "Unique identifier for the user (e.g., Firebase Auth UID)."
        },
        "name": {
          "type": "string",
          "description": "The user's display name."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user profile was created.",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "name",
        "email",
        "createdAt"
      ]
    },
    "Medication": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Medication",
      "type": "object",
      "description": "Represents a medication schedule and inventory for a user.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The unique identifier of the user who owns this medication."
        },
        "name": {
          "type": "string",
          "description": "The name of the medication."
        },
        "dosageAmount": {
          "type": "number",
          "description": "The amount of medication per dose."
        },
        "dosageUnit": {
          "type": "string",
          "description": "The unit of the dosage (e.g., 'pill', 'mg')."
        },
        "times": {
          "type": "array",
          "description": "List of times (HH:mm) when the medication should be taken.",
          "items": {
            "type": "string"
          }
        },
        "startDate": {
          "type": "string",
          "description": "The date when the medication schedule starts.",
          "format": "date-time"
        },
        "totalQuantity": {
          "type": "number",
          "description": "The initial total quantity of medication."
        },
        "remainingQuantity": {
          "type": "number",
          "description": "The current remaining quantity of medication."
        },
        "refillThreshold": {
          "type": "number",
          "description": "The threshold quantity at which a refill alert is triggered."
        },
        "frequency": {
          "type": "string",
          "description": "The frequency of medication intake (e.g., 'daily')."
        }
      },
      "required": [
        "userId",
        "name",
        "dosageAmount",
        "dosageUnit",
        "times",
        "startDate",
        "totalQuantity",
        "remainingQuantity",
        "refillThreshold",
        "frequency"
      ]
    },
    "DoseLog": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "DoseLog",
      "type": "object",
      "description": "Records an instance of a medication dose being taken, missed, or skipped.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The unique identifier of the user who took the dose."
        },
        "medicationId": {
          "type": "string",
          "description": "The ID of the medication this log refers to."
        },
        "name": {
          "type": "string",
          "description": "The name of the medication at the time of the log."
        },
        "status": {
          "type": "string",
          "description": "The status of the dose (e.g., 'taken', 'missed').",
          "enum": ["taken", "missed", "skipped", "pending"]
        },
        "scheduledTime": {
          "type": "string",
          "description": "The original time the dose was scheduled for.",
          "format": "date-time"
        },
        "recordedAt": {
          "type": "string",
          "description": "The time the status was recorded.",
          "format": "date-time"
        },
        "takenAt": {
          "type": "string",
          "description": "The actual time the medication was taken (if applicable).",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "medicationId",
        "name",
        "status",
        "scheduledTime",
        "recordedAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores individual user profiles.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/medicines/{medicineId}",
        "definition": {
          "entityName": "Medication",
          "schema": {
            "$ref": "#/backend/entities/Medication"
          },
          "description": "Stores individual medications for a user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user owner."
            },
            {
              "name": "medicineId",
              "description": "The unique identifier of the medication."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/medicines/{medicineId}/doseLogs/{doseLogId}",
        "definition": {
          "entityName": "DoseLog",
          "schema": {
            "$ref": "#/backend/entities/DoseLog"
          },
          "description": "Stores logs for each dose of a medication.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user owner."
            },
            {
              "name": "medicineId",
              "description": "The unique identifier of the parent medication."
            },
            {
              "name": "doseLogId",
              "description": "The unique identifier of the log entry."
            }
          ]
        }
      }
    ],
    "reasoning": "The 'doseLogs' subcollection is nested under the medicine document to maintain clear ownership and parent-child relationship, while allowing collectionGroup queries for global history tracking."
  }
}
